// Plain list of provider IDs safe for server-side imports (no React components)
// Keep in sync with providers supported in user keys and env mappings
export const PROVIDER_IDS: string[] = [
  "openai",
  "xai",
  "google",
  "anthropic",
  "mistral",
  "perplexity",
  "openrouter",
]

export default PROVIDER_IDS
